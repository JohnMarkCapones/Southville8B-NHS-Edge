<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:Southville8BEdgeUI.ViewModels.Teacher"
             xmlns:ic="using:FluentIcons.Avalonia"
             x:Class="Southville8BEdgeUI.Views.Teacher.SettingsView"
             x:DataType="vm:SettingsViewModel"
             Background="{DynamicResource PageBackgroundBrush}">
    <UserControl.Styles>
        <!-- CARD -->
        <Style Selector="Border.card">
            <Setter Property="Background" Value="{DynamicResource CardBackgroundBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="BoxShadow" Value="0 2 8 0 #10000000"/>
        </Style>
        <Style Selector="Border.card.tablet">
            <Setter Property="Padding" Value="16"/>
        </Style>
        <Style Selector="Border.card.mobile">
            <Setter Property="Padding" Value="12"/>
        </Style>

        <!-- SECTION HEADER -->
        <Style Selector="TextBlock.section-header">
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
        </Style>
        <Style Selector="TextBlock.section-header.tablet">
            <Setter Property="FontSize" Value="16"/>
        </Style>
        <Style Selector="TextBlock.section-header.mobile">
            <Setter Property="FontSize" Value="14"/>
        </Style>

        <!-- LABELS & TEXT -->
        <Style Selector="TextBlock.muted">
            <Setter Property="Foreground" Value="{DynamicResource TextMutedBrush}"/>
        </Style>
        <Style Selector="TextBlock.secondary">
            <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>

        <!-- INPUTS -->
        <Style Selector="TextBox, ComboBox, RadioButton, CheckBox, ToggleSwitch">
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
        </Style>
        <Style Selector="TextBox, ComboBox">
            <Setter Property="CornerRadius" Value="6"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="{DynamicResource CardBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
        </Style>
        <Style Selector="TextBox:focus, ComboBox:focus">
            <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
        </Style>

        <!-- BUTTONS -->
        <Style Selector="Button.primary">
            <Setter Property="Background" Value="{DynamicResource SuccessBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource AccentTextOnAccentBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="CornerRadius" Value="6"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
        <Style Selector="Button.outline">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource TextMutedBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="6"/>
            <Setter Property="Padding" Value="12,6"/>
        </Style>
        <Style Selector="Button.outline:pointerover">
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource TextPrimaryBrush}"/>
        </Style>
        <Style Selector="Button.danger">
            <Setter Property="Background" Value="{DynamicResource DangerBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource AccentTextOnAccentBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="CornerRadius" Value="6"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
        <Style Selector="Button.danger:pointerover">
            <Setter Property="Background" Value="{DynamicResource DangerBrush}"/>
        </Style>

        <!-- ACCENT COLOR SELECTORS -->
        <Style Selector="Button.accent-swatch">
            <Setter Property="Width" Value="20"/>
            <Setter Property="Height" Value="20"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>

    </UserControl.Styles>

    <Grid>
        <ScrollViewer x:Name="MainScrollViewer" VerticalScrollBarVisibility="Auto">
            <StackPanel x:Name="MainStackPanel" Spacing="16" Margin="16">
                <!-- Page Header -->
                <Border Classes="card">
                    <Grid x:Name="HeaderGrid" ColumnDefinitions="Auto,*,Auto">
                        <Border Width="36" Height="36" Background="{DynamicResource SuccessBrush}" CornerRadius="8" VerticalAlignment="Center">
                            <ic:FluentIcon Icon="Settings" IconSize="Size20" Foreground="{DynamicResource AccentTextOnAccentBrush}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <StackPanel Grid.Column="1" Spacing="2" VerticalAlignment="Center" Margin="12,0,0,0">
                            <TextBlock Text="Settings" FontSize="16" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}"/>
                            <TextBlock Text="Manage appearance, notifications, and privacy preferences" FontSize="12" Foreground="{DynamicResource TextMutedBrush}"/>
                        </StackPanel>
                        <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center" Margin="12,0,0,0">
                            <Button x:Name="SaveButton" Content="Save" Classes="primary"/>
                            <Button x:Name="ResetButton" Content="Reset" Classes="outline"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Content -->
                <StackPanel x:Name="ContentPanel" Spacing="12" MaxWidth="600">
                    <!-- Change Password -->
                    <Border Classes="card">
                        <StackPanel Spacing="16">
                            <TextBlock Text="Change Password" Classes="section-header"/>
                            <TextBlock Text="Update your account password to keep your account secure" 
                                      Classes="secondary" 
                                      TextWrapping="Wrap" 
                                      Margin="0,0,0,8"/>
                            
                            <StackPanel Spacing="12">
                                <StackPanel Spacing="6">
                                    <TextBlock Text="Current Password" Classes="muted" FontSize="13"/>
                                    <TextBox Text="{Binding CurrentPassword}" 
                                            PasswordChar="●" 
                                            Watermark="Enter your current password"/>
                                </StackPanel>
                                
                                <StackPanel Spacing="6">
                                    <TextBlock Text="New Password" Classes="muted" FontSize="13"/>
                                    <TextBox Text="{Binding NewPassword}" 
                                            PasswordChar="●" 
                                            Watermark="Enter new password"/>
                                </StackPanel>
                                
                                <StackPanel Spacing="6">
                                    <TextBlock Text="Confirm New Password" Classes="muted" FontSize="13"/>
                                    <TextBox Text="{Binding ConfirmPassword}" 
                                            PasswordChar="●" 
                                            Watermark="Confirm new password"/>
                                </StackPanel>
                                
                                <!-- Password Requirements -->
                                <Border Background="{DynamicResource InfoBackgroundBrush}" 
                                       BorderBrush="{DynamicResource InfoBrush}" 
                                       BorderThickness="1" 
                                       CornerRadius="6" 
                                       Padding="12"
                                       Margin="0,4,0,0">
                                    <StackPanel Spacing="4">
                                        <TextBlock Text="Password Requirements:" 
                                                  FontWeight="SemiBold" 
                                                  FontSize="12" 
                                                  Foreground="{DynamicResource TextPrimaryBrush}"/>
                                        <TextBlock Text="• At least 8 characters long" 
                                                  FontSize="11" 
                                                  Foreground="{DynamicResource TextSecondaryBrush}"/>
                                        <TextBlock Text="• Contains uppercase and lowercase letters" 
                                                  FontSize="11" 
                                                  Foreground="{DynamicResource TextSecondaryBrush}"/>
                                        <TextBlock Text="• Includes at least one number" 
                                                  FontSize="11" 
                                                  Foreground="{DynamicResource TextSecondaryBrush}"/>
                                    </StackPanel>
                                </Border>
                                
                                <Button Command="{Binding ChangePasswordCommand}" 
                                       Classes="primary" 
                                       HorizontalAlignment="Left"
                                       Margin="0,8,0,0"
                                       IsEnabled="{Binding !IsChangingPassword}">
                                    <StackPanel Orientation="Horizontal" Spacing="8">
                                        <ic:FluentIcon Icon="LockClosed" IconSize="Size16"/>
                                        <TextBlock Text="{Binding ChangePasswordButtonText}" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Button>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
