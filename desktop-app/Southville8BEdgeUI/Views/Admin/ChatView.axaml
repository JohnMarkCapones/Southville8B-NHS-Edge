<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="using:Southville8BEdgeUI.ViewModels.Admin"
             xmlns:converters="using:Southville8BEdgeUI.Converters"
             mc:Ignorable="d" d:DesignWidth="1280" d:DesignHeight="800"
             x:Class="Southville8BEdgeUI.Views.Admin.ChatView"
             x:DataType="vm:ChatViewModel"
             Background="#F9FAFB">

    <UserControl.Resources>
        <converters:BoolToStringConverter x:Key="OnlineStatusConverter" TrueValue="Online" FalseValue="Offline"/>
    </UserControl.Resources>

    <UserControl.Styles>
        <!-- RESPONSIVE TEXT STYLES -->

        <!-- Main Header -->
        <Style Selector="TextBlock.main-header">
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="Black"/>
        </Style>
        <Style Selector="TextBlock.main-header.tablet">
            <Setter Property="FontSize" Value="16"/>
        </Style>
        <Style Selector="TextBlock.main-header.mobile">
            <Setter Property="FontSize" Value="14"/>
        </Style>

        <!-- Subtitle Text -->
        <Style Selector="TextBlock.subtitle">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="#6B7280"/>
        </Style>
        <Style Selector="TextBlock.subtitle.tablet">
            <Setter Property="FontSize" Value="11"/>
        </Style>
        <Style Selector="TextBlock.subtitle.mobile">
            <Setter Property="FontSize" Value="10"/>
        </Style>

        <!-- Chat Header Name -->
        <Style Selector="TextBlock.chat-header-name">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="Black"/>
        </Style>
        <Style Selector="TextBlock.chat-header-name.tablet">
            <Setter Property="FontSize" Value="14"/>
        </Style>
        <Style Selector="TextBlock.chat-header-name.mobile">
            <Setter Property="FontSize" Value="12"/>
        </Style>

        <!-- Contact Name -->
        <Style Selector="TextBlock.contact-name">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
        <Style Selector="TextBlock.contact-name.tablet">
            <Setter Property="FontSize" Value="13"/>
        </Style>
        <Style Selector="TextBlock.contact-name.mobile">
            <Setter Property="FontSize" Value="12"/>
        </Style>

        <!-- Message Text -->
        <Style Selector="TextBlock.message-text">
            <Setter Property="FontSize" Value="14"/>
        </Style>
        <Style Selector="TextBlock.message-text.tablet">
            <Setter Property="FontSize" Value="13"/>
        </Style>
        <Style Selector="TextBlock.message-text.mobile">
            <Setter Property="FontSize" Value="12"/>
        </Style>

        <!-- Label Text -->
        <Style Selector="TextBlock.label">
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Foreground" Value="#6B7280"/>
        </Style>
        <Style Selector="TextBlock.label.tablet">
            <Setter Property="FontSize" Value="10"/>
        </Style>
        <Style Selector="TextBlock.label.mobile">
            <Setter Property="FontSize" Value="9"/>
        </Style>

        <!-- Light Text -->
        <Style Selector="TextBlock.light">
            <Setter Property="Foreground" Value="#6B7280"/>
        </Style>

        <!-- Empty State Text -->
        <Style Selector="TextBlock.empty-title">
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#6B7280"/>
        </Style>
        <Style Selector="TextBlock.empty-title.tablet">
            <Setter Property="FontSize" Value="16"/>
        </Style>
        <Style Selector="TextBlock.empty-title.mobile">
            <Setter Property="FontSize" Value="14"/>
        </Style>

        <!-- RESPONSIVE CARD STYLES -->

        <!-- Main Card Style -->
        <Style Selector="Border.card">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="BoxShadow" Value="0 2 8 0 #10000000"/>
        </Style>
        <Style Selector="Border.card.tablet">
            <Setter Property="CornerRadius" Value="10"/>
        </Style>
        <Style Selector="Border.card.mobile">
            <Setter Property="CornerRadius" Value="8"/>
        </Style>

        <!-- RESPONSIVE BUTTON STYLES -->

        <!-- Primary Button -->
        <Style Selector="Button.primary">
            <Setter Property="Background" Value="#4F46E5"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Transitions">
                <Transitions>
                    <TransformOperationsTransition Property="RenderTransform" Duration="0:0:0.1"/>
                </Transitions>
            </Setter>
        </Style>
        <Style Selector="Button.primary:pointerover">
            <Setter Property="RenderTransform" Value="scale(1.02)"/>
        </Style>
        <Style Selector="Button.primary.tablet">
            <Setter Property="Padding" Value="14,8"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
        <Style Selector="Button.primary.mobile">
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>
        
        <!-- Secondary Button -->
        <Style Selector="Button.secondary">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#374151"/>
            <Setter Property="BorderBrush" Value="#D1D5DB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Transitions">
                <Transitions>
                    <TransformOperationsTransition Property="RenderTransform" Duration="0:0:0.1"/>
                </Transitions>
            </Setter>
        </Style>
        <Style Selector="Button.secondary:pointerover">
            <Setter Property="RenderTransform" Value="scale(1.02)"/>
            <Setter Property="Background" Value="#F9FAFB"/>
        </Style>
        <Style Selector="Button.secondary.tablet">
            <Setter Property="Padding" Value="14,8"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
        <Style Selector="Button.secondary.mobile">
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>

        <!-- Back Button Style -->
        <Style Selector="Button.back-button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#6B7280"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Transitions">
                <Transitions>
                    <BrushTransition Property="Background" Duration="0:0:0.2"/>
                </Transitions>
            </Setter>
        </Style>
        <Style Selector="Button.back-button:pointerover">
            <Setter Property="Background" Value="#F3F4F6"/>
        </Style>
        <Style Selector="Button.back-button.tablet">
            <Setter Property="Padding" Value="6"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
        <Style Selector="Button.back-button.mobile">
            <Setter Property="Padding" Value="4"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>

        <!-- Conversation Item Styles -->
        <Style Selector="Button.conversation-item">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="VerticalContentAlignment" Value="Stretch"/>
            <Setter Property="Transitions">
                <Transitions>
                    <BrushTransition Property="Background" Duration="0:0:0.2"/>
                </Transitions>
            </Setter>
        </Style>
        <Style Selector="Button.conversation-item:pointerover">
            <Setter Property="Background" Value="#F3F4F6"/>
        </Style>
        <Style Selector="Button.conversation-item.selected">
            <Setter Property="Background" Value="#EBF8FF"/>
            <Setter Property="BorderBrush" Value="#3B82F6"/>
            <Setter Property="BorderThickness" Value="0,0,3,0"/>
        </Style>
        <Style Selector="Button.conversation-item.tablet">
            <Setter Property="Padding" Value="14"/>
        </Style>
        <Style Selector="Button.conversation-item.mobile">
            <Setter Property="Padding" Value="12"/>
        </Style>

        <!-- Message Bubble Styles -->
        <Style Selector="Border.message-bubble">
            <Setter Property="CornerRadius" Value="18"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="MaxWidth" Value="320"/>
            <Setter Property="Margin" Value="0,2"/>
        </Style>
        <Style Selector="Border.message-bubble.tablet">
            <Setter Property="CornerRadius" Value="16"/>
            <Setter Property="Padding" Value="10,7"/>
            <Setter Property="MaxWidth" Value="280"/>
        </Style>
        <Style Selector="Border.message-bubble.mobile">
            <Setter Property="CornerRadius" Value="14"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="MaxWidth" Value="240"/>
        </Style>

        <Style Selector="Border.message-sent">
            <Setter Property="Background" Value="#4F46E5"/>
            <Setter Property="HorizontalAlignment" Value="Right"/>
        </Style>

        <Style Selector="Border.message-received">
            <Setter Property="Background" Value="#F3F4F6"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
        </Style>

        <!-- Chat Icon Button Styles -->
        <Style Selector="Button.chat-icon">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#6B7280"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Transitions">
                <Transitions>
                    <BrushTransition Property="Background" Duration="0:0:0.2"/>
                </Transitions>
            </Setter>
        </Style>
        <Style Selector="Button.chat-icon:pointerover">
            <Setter Property="Background" Value="#F3F4F6"/>
        </Style>
        <Style Selector="Button.chat-icon.tablet">
            <Setter Property="Padding" Value="6"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
        <Style Selector="Button.chat-icon.mobile">
            <Setter Property="Padding" Value="4"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>

        <!-- RESPONSIVE INPUT STYLES -->

        <Style Selector="TextBox, ComboBox">
            <Setter Property="Padding" Value="12"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="BorderBrush" Value="#D1D5DB"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
        <Style Selector="TextBox.tablet, ComboBox.tablet">
            <Setter Property="Padding" Value="10"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
        <Style Selector="TextBox.mobile, ComboBox.mobile">
            <Setter Property="Padding" Value="8"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>

        <!-- Message Input Special Styling -->
        <Style Selector="TextBox.message-input">
            <Setter Property="CornerRadius" Value="24"/>
            <Setter Property="Padding" Value="16,12"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
        <Style Selector="TextBox.message-input.tablet">
            <Setter Property="Padding" Value="14,10"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
        <Style Selector="TextBox.message-input.mobile">
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>

        <!-- Send Button Special Styling -->
        <Style Selector="Button.send-button">
            <Setter Property="CornerRadius" Value="24"/>
            <Setter Property="Padding" Value="20,12"/>
        </Style>
        <Style Selector="Button.send-button.tablet">
            <Setter Property="Padding" Value="18,10"/>
        </Style>
        <Style Selector="Button.send-button.mobile">
            <Setter Property="Padding" Value="16,8"/>
        </Style>

        <!-- RESPONSIVE LAYOUT STYLES -->

        <Style Selector="Grid.main-content">
            <Setter Property="Background" Value="#F9FAFB"/>
        </Style>
        <Style Selector="Grid.main-content-tablet">
            <Setter Property="Background" Value="#F9FAFB"/>
        </Style>
        <Style Selector="Grid.main-content-mobile">
            <Setter Property="Background" Value="#F9FAFB"/>
        </Style>

        <!-- Avatar Styles -->
        <Style Selector="Border.avatar">
            <Setter Property="Width" Value="40"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="CornerRadius" Value="20"/>
        </Style>
        <Style Selector="Border.avatar.tablet">
            <Setter Property="Width" Value="36"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="CornerRadius" Value="18"/>
        </Style>
        <Style Selector="Border.avatar.mobile">
            <Setter Property="Width" Value="32"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="CornerRadius" Value="16"/>
        </Style>

        <!-- Online Indicator Styles -->
        <Style Selector="Border.online-indicator">
            <Setter Property="Width" Value="6"/>
            <Setter Property="Height" Value="6"/>
            <Setter Property="CornerRadius" Value="3"/>
            <Setter Property="Background" Value="#10B981"/>
        </Style>
        <Style Selector="Border.online-indicator.tablet">
            <Setter Property="Width" Value="5"/>
            <Setter Property="Height" Value="5"/>
            <Setter Property="CornerRadius" Value="2.5"/>
        </Style>
        <Style Selector="Border.online-indicator.mobile">
            <Setter Property="Width" Value="4"/>
            <Setter Property="Height" Value="4"/>
            <Setter Property="CornerRadius" Value="2"/>
        </Style>

        <!-- SCROLLBAR STYLES -->
        <!-- Custom ScrollBar styling for better UX -->
        <Style Selector="ScrollBar">
            <Setter Property="AllowAutoHide" Value="True"/>
            <Setter Property="Margin" Value="0"/>
        </Style>
        
        <Style Selector="ScrollBar:vertical">
            <Setter Property="Width" Value="12"/>
            <Setter Property="Margin" Value="2,0,0,0"/>
        </Style>
        
        <Style Selector="ScrollBar:horizontal">
            <Setter Property="Height" Value="12"/>
            <Setter Property="Margin" Value="0,2,0,0"/>
        </Style>
        
        <!-- ScrollBar Track Styling -->
        <Style Selector="ScrollBar /template/ Track#PART_Track">
            <Setter Property="IsDirectionReversed" Value="True"/>
        </Style>
        
        <!-- ScrollBar Thumb Styling -->
        <Style Selector="ScrollBar /template/ Thumb">
            <Setter Property="Background" Value="#D1D5DB"/>
            <Setter Property="CornerRadius" Value="6"/>
            <Setter Property="Margin" Value="1"/>
            <Setter Property="Transitions">
                <Transitions>
                    <BrushTransition Property="Background" Duration="0:0:0.2"/>
                </Transitions>
            </Setter>
        </Style>
        
        <Style Selector="ScrollBar /template/ Thumb:pointerover">
            <Setter Property="Background" Value="#9CA3AF"/>
        </Style>
        
        <Style Selector="ScrollBar /template/ Thumb:pressed">
            <Setter Property="Background" Value="#6B7280"/>
        </Style>
        
        <!-- ScrollBar Button Styling (Up/Down arrows) -->
        <Style Selector="ScrollBar /template/ RepeatButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="#9CA3AF"/>
        </Style>
        
        <Style Selector="ScrollBar /template/ RepeatButton:pointerover">
            <Setter Property="Background" Value="#F3F4F6"/>
            <Setter Property="Foreground" Value="#6B7280"/>
        </Style>
        
        <!-- Hide ScrollBar arrows for cleaner look (optional) -->
        <Style Selector="ScrollBar /template/ RepeatButton#PART_LineUpButton">
            <Setter Property="IsVisible" Value="False"/>
        </Style>
        
        <Style Selector="ScrollBar /template/ RepeatButton#PART_LineDownButton">
            <Setter Property="IsVisible" Value="False"/>
        </Style>
        
        <Style Selector="ScrollBar /template/ RepeatButton#PART_LineLeftButton">
            <Setter Property="IsVisible" Value="False"/>
        </Style>
        
        <Style Selector="ScrollBar /template/ RepeatButton#PART_LineRightButton">
            <Setter Property="IsVisible" Value="False"/>
        </Style>

        <!-- ScrollViewer styling -->
        <Style Selector="ScrollViewer">
            <Setter Property="HorizontalScrollBarVisibility" Value="Auto"/>
            <Setter Property="VerticalScrollBarVisibility" Value="Auto"/>
        </Style>
        
        <!-- Enhanced ScrollViewer for better mobile experience -->
        <Style Selector="ScrollViewer.mobile">
            <Setter Property="HorizontalScrollBarVisibility" Value="Hidden"/>
            <Setter Property="VerticalScrollBarVisibility" Value="Auto"/>
        </Style>
        
        <Style Selector="ScrollViewer.tablet">
            <Setter Property="HorizontalScrollBarVisibility" Value="Auto"/>
            <Setter Property="VerticalScrollBarVisibility" Value="Auto"/>
        </Style>
    </UserControl.Styles>

    <Grid x:Name="MainGrid" Classes="main-content" ColumnDefinitions="360,*" Margin="24">
        <!-- Conversations List -->
        <Border x:Name="ConversationsCard" Grid.Column="0" Classes="card" Margin="0,0,12,0">
            <Grid RowDefinitions="Auto,Auto,*">
                <!-- Header -->
                <Border x:Name="ConversationsHeader" Grid.Row="0" Padding="20,20,20,16" BorderBrush="#E5E7EB" BorderThickness="0,0,0,1">
                    <Grid ColumnDefinitions="*,Auto">
                        <StackPanel Grid.Column="0">
                            <TextBlock x:Name="ConversationsHeaderText" Text="Messages" Classes="main-header"/>
                            <TextBlock x:Name="ConversationsSubtitleText" Text="Admin Communication Hub" Classes="subtitle"/>
                        </StackPanel>
                        <Button x:Name="NewChatButton" Grid.Column="1" Content="➕ New" Classes="primary" 
                                Command="{Binding StartNewChatCommand}"
                                VerticalAlignment="Center"/>
                    </Grid>
                </Border>

                <!-- Search and Filter -->
                <Border Grid.Row="1" Padding="16" BorderBrush="#E5E7EB" BorderThickness="0,0,0,1">
                    <StackPanel Spacing="12">
                        <TextBox x:Name="SearchTextBox" Watermark="Search conversations..." 
                                 Text="{Binding SearchText}"
                                 CornerRadius="8"/>
                        <ComboBox x:Name="UserTypeComboBox" PlaceholderText="All Users" 
                                  ItemsSource="{Binding UserTypeOptions}"
                                  SelectedItem="{Binding SelectedUserType}"
                                  HorizontalAlignment="Stretch"/>
                    </StackPanel>
                </Border>

                <!-- Conversations List and Empty State -->
                <Grid Grid.Row="2">
                    <!-- Enhanced ScrollViewer for Conversations List -->
                    <ScrollViewer x:Name="ConversationsScrollViewer"
                                  VerticalScrollBarVisibility="Auto" 
                                  HorizontalScrollBarVisibility="Disabled"
                                  IsVisible="{Binding HasConversations}"
                                  AllowAutoHide="True">
                        <ItemsControl ItemsSource="{Binding FilteredConversations}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate DataType="vm:ChatConversationViewModel">
                                    <Button Classes="conversation-item"
                                            Classes.selected="{Binding $parent[UserControl].((vm:ChatViewModel)DataContext).SelectedConversation, Converter={x:Static ObjectConverters.Equal}, ConverterParameter={Binding}}"
                                            Command="{Binding $parent[UserControl].((vm:ChatViewModel)DataContext).SelectConversationCommand}"
                                            CommandParameter="{Binding}">
                                        
                                        <Grid ColumnDefinitions="Auto,*,Auto">
                                            <!-- Avatar -->
                                            <Border Grid.Column="0" Classes="avatar"
                                                    Background="{Binding RoleColor}" 
                                                    Margin="0,0,12,0">
                                                <TextBlock Text="{Binding ContactInitials}" 
                                                           FontSize="14" 
                                                           FontWeight="SemiBold" 
                                                           Foreground="White"
                                                           HorizontalAlignment="Center" 
                                                           VerticalAlignment="Center"/>
                                            </Border>

                                            <!-- Content -->
                                            <StackPanel Grid.Column="1" Spacing="4">
                                                <Grid ColumnDefinitions="*,Auto">
                                                    <TextBlock Grid.Column="0" Text="{Binding ContactName}" 
                                                               Classes="contact-name"/>
                                                    <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="4">
                                                        <Border Classes="online-indicator"
                                                                VerticalAlignment="Center"
                                                                IsVisible="{Binding IsOnline}"/>
                                                        <TextBlock Text="{Binding ContactRole}" 
                                                                   FontSize="10" 
                                                                   Classes="light"/>
                                                    </StackPanel>
                                                </Grid>
                                                <TextBlock Text="{Binding LastMessage}" 
                                                           Classes="light" 
                                                           FontSize="12" 
                                                           TextWrapping="Wrap"
                                                           MaxLines="2"/>
                                                <TextBlock Text="{Binding LastMessageTimeText}" 
                                                           Classes="light" 
                                                           FontSize="10"/>
                                            </StackPanel>

                                            <!-- Unread Badge -->
                                            <Border Grid.Column="2" 
                                                    Background="#EF4444" 
                                                    CornerRadius="10" 
                                                    MinWidth="20" 
                                                    Height="20"
                                                    IsVisible="{Binding HasUnreadMessages}"
                                                    VerticalAlignment="Top">
                                                <TextBlock Text="{Binding UnreadCount}" 
                                                           FontSize="10" 
                                                           FontWeight="Bold" 
                                                           Foreground="White"
                                                           HorizontalAlignment="Center" 
                                                           VerticalAlignment="Center"/>
                                            </Border>
                                        </Grid>
                                    </Button>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>

                    <!-- Empty State -->
                    <StackPanel Spacing="16" 
                                HorizontalAlignment="Center" 
                                VerticalAlignment="Center"
                                IsVisible="{Binding !HasConversations}">
                        <Path Data="M8 12H16M8 8H16M8 16H12M6 20H18C19.1046 20 20 19.1046 20 18V6C20 4.89543 19.1046 4 18 4H6C4.89543 4 4 4.89543 4 6V18C4 19.1046 4.89543 20 6 20Z" 
                              Stroke="#9CA3AF" 
                              StrokeThickness="2" 
                              Width="48" 
                              Height="48"/>
                        <TextBlock x:Name="NoMessagesText" Text="No conversations found" Classes="empty-title"/>
                        <TextBlock Text="Try adjusting your search criteria" Classes="subtitle"/>
                    </StackPanel>
                </Grid>
            </Grid>
        </Border>

        <!-- Chat Area -->
        <Border x:Name="ChatCard" Grid.Column="1" Classes="card" Margin="12,0,0,0">
            <Grid>
                <!-- Selected Conversation Chat -->
                <Grid RowDefinitions="Auto,*,Auto" IsVisible="{Binding HasSelectedConversation}">
                    <!-- Chat Header -->
                    <Border x:Name="ChatHeader" Grid.Row="0" Padding="20" BorderBrush="#E5E7EB" BorderThickness="0,0,0,1">
                        <Grid ColumnDefinitions="Auto,Auto,*,Auto">
                            <!-- Back Button (only visible on mobile/tablet) -->
                            <Button x:Name="BackButton" Grid.Column="0" Content="←" Classes="back-button" 
                                    VerticalAlignment="Center" Margin="0,0,12,0"
                                    IsVisible="False"/>
                            
                            <!-- Contact Info -->
                            <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="12">
                                <Border Classes="avatar"
                                        Background="{Binding SelectedConversation.RoleColor, FallbackValue=#6B7280}">
                                    <TextBlock Text="{Binding SelectedConversation.ContactInitials, FallbackValue=''}" 
                                               FontSize="14" 
                                               FontWeight="SemiBold" 
                                               Foreground="White"
                                               HorizontalAlignment="Center" 
                                               VerticalAlignment="Center"/>
                                </Border>
                                <StackPanel VerticalAlignment="Center">
                                    <TextBlock x:Name="ChatHeaderNameText" 
                                               Text="{Binding SelectedConversation.ContactName, FallbackValue='No Conversation Selected'}" 
                                               Classes="chat-header-name"/>
                                    <StackPanel Orientation="Horizontal" Spacing="6">
                                        <Border Classes="online-indicator"
                                                VerticalAlignment="Center"
                                                IsVisible="{Binding SelectedConversation.IsOnline, FallbackValue=False}"/>
                                        <TextBlock x:Name="ChatHeaderRoleText" 
                                                   Text="{Binding SelectedConversation.ContactRole, FallbackValue=''}" 
                                                   FontSize="12" 
                                                   Classes="light"/>
                                        <TextBlock Text="{Binding SelectedConversation.IsOnline, Converter={StaticResource OnlineStatusConverter}, FallbackValue=''}" 
                                                   FontSize="12" 
                                                   Classes="light"/>
                                    </StackPanel>
                                </StackPanel>
                            </StackPanel>

                            <!-- Chat Actions -->
                            <StackPanel x:Name="ChatHeaderButtons" Grid.Column="3" Orientation="Horizontal" Spacing="8">
                                <Button x:Name="CallButton" Content="📞" Classes="chat-icon"/>
                                <Button x:Name="VideoButton" Content="📹" Classes="chat-icon"/>
                                <Button x:Name="InfoButton" Content="ℹ️" Classes="chat-icon"/>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <!-- Enhanced Messages ScrollViewer -->
                    <ScrollViewer x:Name="MessagesScrollViewer"
                                  Grid.Row="1" 
                                  VerticalScrollBarVisibility="Auto" 
                                  HorizontalScrollBarVisibility="Disabled"
                                  Padding="20"
                                  AllowAutoHide="True">
                        <ItemsControl ItemsSource="{Binding SelectedConversation.Messages, FallbackValue={x:Null}}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate DataType="vm:ChatMessageViewModel">
                                    <StackPanel Margin="0,8">
                                        <Border Classes="message-bubble"
                                                Classes.message-sent="{Binding IsFromCurrentUser}"
                                                Classes.message-received="{Binding !IsFromCurrentUser}"
                                                Background="{Binding MessageBackground}">
                                            <TextBlock Text="{Binding Content}" 
                                                       Foreground="{Binding MessageTextColor}"
                                                       TextWrapping="Wrap"
                                                       Classes="message-text"/>
                                        </Border>
                                        <TextBlock Text="{Binding TimestampText}" 
                                                   FontSize="10" 
                                                   Classes="light"
                                                   HorizontalAlignment="{Binding MessageAlignment}"
                                                   Margin="12,2,12,0"/>
                                    </StackPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>

                    <!-- Message Input -->
                    <Border Grid.Row="2" Padding="20" BorderBrush="#E5E7EB" BorderThickness="0,1,0,0">
                        <Grid x:Name="MessageInputGrid" ColumnDefinitions="*,Auto">
                            <TextBox x:Name="MessageTextBox" Grid.Column="0" 
                                     Text="{Binding NewMessage}"
                                     Watermark="Type a message..."
                                     Classes="message-input"
                                     Margin="0,0,12,0">
                                <TextBox.KeyBindings>
                                    <KeyBinding Gesture="Enter" Command="{Binding SendMessageCommand}"/>
                                </TextBox.KeyBindings>
                            </TextBox>
                            <Button x:Name="SendButton" Grid.Column="1" 
                                    Content="Send"
                                    Classes="primary send-button"
                                    Command="{Binding SendMessageCommand}"/>
                        </Grid>
                    </Border>
                </Grid>

                <!-- No Conversation Selected -->
                <StackPanel Spacing="16" 
                            HorizontalAlignment="Center" 
                            VerticalAlignment="Center"
                            IsVisible="{Binding !HasSelectedConversation}">
                    <Path Data="M8 12H16M8 8H16M8 16H12M6 20H18C19.1046 20 20 19.1046 20 18V6C20 4.89543 19.1046 4 18 4H6C4.89543 4 4 4.89543 4 6V18C4 19.1046 4.89543 20 6 20Z" 
                          Stroke="#9CA3AF" 
                          StrokeThickness="2" 
                          Width="64" 
                          Height="64"/>
                    <TextBlock x:Name="NoConversationTitleText" Text="Select a conversation" Classes="empty-title"/>
                    <TextBlock x:Name="NoConversationSubtitleText" Text="Choose a conversation from the list to start messaging" Classes="subtitle"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>